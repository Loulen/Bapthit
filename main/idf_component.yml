dependencies:
  espressif/arduino-esp32:
    git: "https://github.com/espressif/arduino-esp32.git"
  # Components removed from IDF 6.0 but still required by arduino-esp32's
  # transitive dependency tree.  These will be pulled from the registry.
  espressif/mqtt:
    version: "^1.0"
